{
  "name": "compass-platform",
  "private": true,
  "scripts": {
    "dev": "pnpm -r dev",
    "dev:compass": "pnpm --filter @compass/app dev",
    "dev:voice-lab": "pnpm --filter @compass/voice-lab dev",
    "dev:studio": "pnpm --filter @compass/studio dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "typescript": "^5.4.3"
  }
}
